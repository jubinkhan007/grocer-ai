import 'package:flutter/material.dart';
import 'package:flutter/services.dart';

/// ===== GLOBAL DESIGN TOKENS (from Figma) =====
const bgPage         = Color(0xFFF4F6F6); // page background
const tealStatus     = Color(0xFF002C2E); // dark strip behind status bar
const tealHeader     = Color(0xFF33595B); // header bar, CTA buttons, selected radio ring
const textPrimary    = Color(0xFF212121); // almost-black
const textSecondary  = Color(0xFF4D4D4D); // mid gray body labels
const textMuted      = Color(0xFF6A6A6A); // invoice row label gray
const borderHairline = Color(0xFFE9E9E9); // 1px line (inputs, dividers)
const borderCard     = Color(0xFFB0BFBF); // timing slot borders
const chipBg         = Color(0xFFF5F5F5); // pill background
const cardBg         = Color(0xFFFEFEFE); // white cards
const errorRed       = Color(0xFFBA4012); // "- $15"
const shadowPill     = Color(0x1915224F); // rgba-ish shadow under CTA pill & sheet handles
const maxChipBg      = Color(0xFFE6EAEB); // "Max: $6.50" tag chip bg
const maxChipLabel   = Color(0xFF4D4D4D);
const maxChipValue   = tealHeader;
const linkTeal       = Color(0xFF547476); // "Change"

const chipBorderGrey = Color(0xFFE6EAEB); // 0.5px stroke for payment rows
const fieldStrokeGrey = Color(0xFFE9E9E9); // subtle outline in inputs
const pillBg = Colors.white;
const pillRadius29 = Radius.circular(29);
const pillRadius128 = Radius.circular(128);

const pageBg = Color(0xFFF4F6F6); // overall page bg
const statusTeal = Color(0xFF002C2E); // dark strip behind system status
const headerTeal = Color(0xFF33595B); // app bar bg
const chipBorder = Color(0xFFE9E9E9); // light border color (used elsewhere)
const bubbleBg = Color(0xFFE6EAEB); // gray bg for the 32x32 location icon bubble
const dividerGrey = Color(0xFFE6EAEB); // row divider in saved addresses
const sheetShadow = Color(0x1A000000); // rgba(0,0,0,0.10) for dropdown shadow

