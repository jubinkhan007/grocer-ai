import 'package:flutter/material.dart';

/// Brand palette (adjust to Figma)
class AppColors {
  // Seed & brand
  static const seed = Color(0xFF2E6CF6); // GrocerAI primary (example)
  static const onSeed = Colors.white;

  // Neutrals
  static const neutral0 = Color(0xFFFFFFFF);
  static const neutral10 = Color(0xFFF7F8FA);
  static const neutral20 = Color(0xFFEAECEF);
  static const neutral40 = Color(0xFFB8C0CC);
  static const neutral60 = Color(0xFF7B8794);
  static const neutral80 = Color(0xFF3D4854);
  static const neutral90 = Color(0xFF1F2933);

  // Semantics
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);

  // Category tints (for “Analysis”, chips, etc.)
  static const fruits = Color(0xFFFF6B6B);
  static const vegetables = Color(0xFF10B981);
  static const dairy = Color(0xFF60A5FA);
  static const grains = Color(0xFFF59E0B);
}
